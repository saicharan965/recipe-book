services:
  - name: frontend
    type: web
    runtime: static
    buildCommand: |
      npm install
      npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 4200

  - name: backend
    type: web
    runtime: static
    buildCommand: npm install
    startCommand: npm run server
